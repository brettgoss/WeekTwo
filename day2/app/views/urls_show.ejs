<!DOCTYPE html>
<html lang="en">
<% include ../views/partials/_head %>
<body>
  <div class="container">

  <header>
    <% include ../views/partials/_header %>
  </header>
    <ul>
      <%  %>
        <li><%= key %> -> <a href="/u/<%= key %>"><%= urls[key] %></a>
          <form method="POST" action="/urls/<%= key %>/delete">
            <button>Delete</button>
          </form>
          <form action="/urls/<%= key %>/edit" method="POST">
            <input type="text" name="edit" value="<%= urls[key] %>" />
            <button>Edit</button>
          </form>
        </li>
      <% %>
    </ul>
  </div>
  </body>
</html>
