<!DOCTYPE html>
<html lang="en">
<% include ../views/partials/_head %>
<body>
  <div class="container">

  <header>
    <% include ../views/partials/_header %>
  </header>
  <h2>Edit URL</h2>
    <ul>
      Short URL:
        <li><%= key %></li>
        Full URL:
        <li><a href="/u/<%= key %>"><%= urls[key] %></a></li>
          <form method="POST" action="/urls/<%= key %>/edit" >
            <input style="width: 300px" type="text" name="edit" value="<%= urls[key] %>" />
            <button>Update</button>
          </form>
          <form class="u-cf" method="POST" action="/urls/<%= key %>/delete">
            <button>Delete</button>
          </form>
    </ul>
  </div>
  </body>
</html>
