http://www.example.com/images/logo.png

Verbs:
GET //retrieves a specific request
POST //Adds a new resource
PUT //Replaces a resource with a new one
PATCH //Adds only the differences between the two resources
DELETE //Removes a resource


Path:
GET '/'
POST '/signup'
PUT
